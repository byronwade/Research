# Bootstrap import

This temporary directory contains a checksum-verified, base64-split archive of the complete V6 Research specification. A one-time GitHub Actions import rewrites `main` as the canonical organized repository and removes this directory from reachable history.
