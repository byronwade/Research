name: Disabled canonical Research V6 importer

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Report disabled importer
        run: echo "This one-time V6 import workflow is disabled because Research is now materialized on main."
