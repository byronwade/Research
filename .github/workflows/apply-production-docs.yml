name: Disabled one-time production documentation transport

on:
  workflow_dispatch:

permissions: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - run: echo 'This one-time transport has been superseded by direct, reviewed documentation commits.'
