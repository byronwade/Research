# Security verification matrix

This matrix turns the threat model into owned implementation and release evidence. Controls are defense in depth; one passing layer does not cancel a failure in another.

| Threat or failure | Preventive controls | Required verification | Primary slice |
|---|---|---|---|
| Cross-tenant data access | Server-side Project authorization, tenant-scoped queries, cache partitioning, least-privilege service roles | Negative API, retrieval, export, publication, workflow, and support tests | `identity-02`, `enterprise-25` |
| Broken object authorization | Resource policy checks on every read and mutation; opaque identifiers are not treated as authorization | ID substitution and role downgrade suites | `identity-02` |
| Indirect prompt injection | Untrusted-source boundary, instruction/data separation, tool allowlists, read-only defaults, taint and provenance | Malicious PDF, webpage, issue, email, and repository fixtures | `agents-15`, `enterprise-25` |
| Intent or approval-policy override | Intent, clarification, preflight, and approval policy enforced by application code, not source text or model prose | Malicious source and connector fixtures attempt to alter mode, approval class, tool policy, source scope, or receipt validation | `chat-08`, `research-13`, `enterprise-25` |
| Approval fatigue or delegated-trust abuse | Scoped grants, approval-load budgets, batch equivalence checks, hard-stop action classes, revocation, stale receipt rejection, and mutation-boundary fail-closed enforcement | Repeated low-risk prompt, batch widening, expired grant, revoked grant, source-scope drift, connector widening, new destination, larger budget, and destructive-action fixtures | `commercial-24`, `enterprise-25`, `conformance-26` |
| Abuse prevention false negatives or false positives | AbusePolicies, deterministic abuse preflight, tenant-scoped AbuseThrottles, provider-policy checks, content-safety normalization, review ActionCards, appeals, false-positive observations, emergency controls, and content-minimized telemetry | Source acquisition abuse, publication spam, GitHub PR spam, connector write spam, notification spam, API fanout, MCP abuse, recipe runaway loops, provider-policy denial, content-safety block, legitimate high-volume research, review reversal, and appeal fixtures | `enterprise-25`, `conformance-26` |
| Excessive agent authority | Short-lived scoped capabilities, approval gates, no self-approval, bounded budgets | Attempts to publish, write repositories, send data, change billing, or expand scopes | `agents-15`, `github-20`, `enterprise-25` |
| SSRF and unsafe acquisition | URL parsing, DNS/IP policy, redirect revalidation, egress controls, content limits | Private-network, metadata-service, redirect, DNS rebinding, and oversized-response tests | `web-12`, `enterprise-25` |
| Malicious uploads and parsers | Byte-signature inspection, archive limits, malware checks, isolated parsers, resource quotas | Polyglot, decompression bomb, malformed media, macro, and parser escape fixtures | `parsing-06` |
| Web session attacks | Secure cookies, CSRF protection, origin checks, CSP, controlled CORS, output encoding | ASVS-oriented browser and API security suite | `identity-02`, `workspace-04`, `enterprise-25` |
| Webhook forgery or replay | Signature verification over raw bytes, timestamp window, replay store, idempotency | Invalid signature, old timestamp, duplicate, reordered, and body-mutation tests | `connectors-21`, `platform-22` |
| Credential leakage | Secret manager, encrypted credential records, redaction, scoped tokens, short lifetimes | Repository, build, log, telemetry, support-export, prompt, and client-bundle scans | `foundation-01`, `enterprise-25` |
| Supply-chain compromise | Full-SHA action pins, lockfiles, review policy, SBOM, provenance, vulnerability and license gates | Tampered dependency, unpinned action, vulnerable transitive package, and provenance checks | `foundation-01`, `enterprise-25` |
| Cache or search leakage | Authorization before retrieval, tenant/model/index cache keys, no shared private embeddings | Cross-tenant cache, reranker, vector, and stale-permission tests | `indexing-07`, `enterprise-25` |
| Local cache or offline sync leakage | DeviceCapabilityProfile minimization, LocalCachePolicy enforcement, Project-scoped manifests, no credentials or prohibited private content in browser storage, offline lease expiry, reconnect reauthorization, expected-version checks, high-risk offline-action denial, and SyncConflict review | Cross-tenant local-cache manifest read, offline draft redaction, prohibited action queue rejection, revoked membership reconnect, policy-change invalidation, storage-eviction recovery, sync conflict redaction, device revocation, sign-out clear behavior, service-worker stale-cache, and no raw user-agent or local-content telemetry tests | `workspace-04`, `enterprise-25`, `conformance-26` |
| Native companion or browser extension overreach | Optional install, minimal extension and desktop permissions, active-tab user gestures, Project-scoped grants, NativeContextPacket minimization, file-watch path bounds, no ambient capture, server-owned preflight, signed update policy, emergency revocation, and content-minimized diagnostics | Cross-Project grant read, active-tab without gesture, selected-text leakage, broad host permission, clipboard scrape, screenshot capture, browser-history access, keylogging, camera or microphone access, folder-watch path escape, symlink escape, stale grant queue replay, revoked extension deep link, unsigned update, and companion telemetry leakage tests | `workspace-04`, `platform-22`, `enterprise-25`, `conformance-26` |
| Hidden personalization or preference leakage | Scoped PreferencePolicies, visible Preference Center controls, content-minimized PreferenceObservations, explanation and correction paths, reset/export, model-context minimization, dependency invalidation, and no preference-as-evidence behavior | Cross-Project PreferenceItem read, hidden-profile creation attempt, private-context explanation redaction, stale preference after source revocation, policy-managed disablement, reset/export completeness, model-context summary minimization, and support/API/MCP projection tests | `workspace-04`, `enterprise-25`, `conformance-26` |
| Spatial layout metadata leakage | Projection-safe Workset state, restore-time authorization, redacted tombstones, stale-resource invalidation, no screenshots, no browser history, no operating-system window capture | Cross-tenant Workset read, stale pane restore, revoked source/document restore, layout suggestion redaction, support diagnostic minimization, and no-ambient-capture tests | `workspace-04`, `platform-22`, `conformance-26` |
| Health diagnostic or repair leakage | Viewer-scoped health snapshots, support-grant checks, redacted reason categories, typed repair playbooks, deterministic preflight, expected versions, idempotency, approval receipts, side-effect ledgers, no screenshots, no browser history, no operating-system state capture | Cross-tenant HealthFinding read, revoked source/document finding, SupportDiagnosticBundle minimization, support diagnostic minimization, repair dry-run authorization, stale RepairRun mutation, unsafe external write, and no-ambient-capture tests | `workspace-04`, `platform-22`, `conformance-26` |
| Support access leakage or misuse | Metadata-first SupportDiagnosticBundles, scoped SupportAccessRequests, time-bounded SupportAccessSessions, step-up, expiry, revocation, self-approval rejection, immutable audit, break-glass review, and export minimization | Cross-tenant bundle read, overbroad bundle generation, stale session access, revoked session access, self-approval, arbitrary export, arbitrary SQL, hidden mutation, private-content model evaluation, and break-glass review tests | `enterprise-25`, `conformance-26` |
| Accessibility or locale metadata leakage | AccessibilityProfile and LocaleProfile minimization, locale-neutral APIs, language and direction metadata ownership, translation provider policy, accessible-output manifests, no assistive-technology identity capture, and no raw content in locale diagnostics | Cross-tenant profile read, private content in language diagnostics, translation without provider policy, localized field mutating canonical value, TranslationArtifact treated as evidence, RTL citation mismatch, and accessible-output manifest leakage tests | `enterprise-25`, `conformance-26` |
| Scenario simulation leakage or unsafe apply | Viewer-scoped ScenarioInputSnapshots, content minimization, live-test classification, stale-plan invalidation, apply-candidate revalidation, owning-service mutation boundaries, side-effect ledgers, no ambient OS capture | Cross-tenant Scenario read, revoked source/document simulation, hidden dependent redaction, live-write mislabeled as no-side-effect, stale apply candidate, and simulated-effect mutation attempts | `workspace-04`, `platform-22`, `conformance-26` |
| Reversible Work leakage or unsafe recovery | Viewer-scoped recovery capabilities, Project history minimization, expected-version revalidation, side-effect reconciliation, compensation plans, irreversible-effect labels, no ambient OS capture | Cross-tenant Project history read, hidden recovery option redaction, stale recovery mutation, duplicated external write replay, missing irreversible warning, unauthorized compensation, and reconciliation-before-retry tests | `workspace-04`, `enterprise-25`, `conformance-26` |
| Public/private projection leakage | One canonical revision, explicit projection policy, privacy and rights checks | Hidden-block, private-source, revoked-source, and publication snapshot fixtures | `publishing-18`, `enterprise-25` |
| Collaboration metadata leakage | Collaboration-aware projection, authorization at create/notify/read/export/publication, support-safe diagnostics | Private comment, inaccessible mention, reviewer identity, unresolved objection, stale anchor, and public export fixtures | `documents-10`, `publishing-18`, `enterprise-25` |
| Source-change maintenance overreach or stale-output miss | Immutable SourceVersions, SourceFreshnessPolicies, content-minimized SourceChangeSets, ClaimRevalidations, Impact Reports, typed MaintenancePatchProposals, MaintenanceActionCards, expected versions, approval gates, publication/export blockers, and support-safe diagnostics | Hidden rewrite attempt, stale public output, broad patch proposal, private affected-claim leakage, source-revocation revalidation, failed-refresh state, official-reference expiry, maintenance replay, and no-silent-rewrite fixtures | `maintenance-17`, `publishing-18`, `enterprise-25`, `conformance-26` |
| Deletion resurrection | Tombstones, derivative traversal, restore reapplication, cache/index invalidation | Delete, restore, reconnect, and backup-expiry exercises | `maintenance-17`, `enterprise-25`, `conformance-26` |
| Billing or quota abuse | Reservations, immutable usage ledger, signed provider reconciliation, rate and concurrency limits | Duplicate, delayed, corrected, adversarial, and denial-of-wallet scenarios | `commercial-24`, `enterprise-25` |
| Administrative misuse | Separate support plane, SupportAccessSessions, step-up, time-bounded grants, dual control, immutable audit | Break-glass, expired grant, unauthorized repair, stale support access, and evidence-access tests | `enterprise-25`, `conformance-26` |
| Migration or rollback corruption | Expand/backfill/switch/contract, compatibility windows, backups, reconciliation | Mixed-version, interrupted backfill, rollback, restore, and in-flight workflow tests | `persistence-03`, `conformance-26` |

## Evidence format

Each verification record includes control version, test or exercise ID, environment, source commit, artifact digest, configuration version, data fixture, actor, result, affected tenants where applicable, logs or traces, remediation owner, and expiry or next exercise date.

## Release rule

A failed tenant-isolation, credential, destructive-action, publication-privacy, deletion, restore, reversal, compensation, or irreversible-effect labeling control blocks general availability. Risk acceptance requires an authorized owner, bounded scope, compensating control, expiry, and tracked remediation; some invariants are non-waivable.
